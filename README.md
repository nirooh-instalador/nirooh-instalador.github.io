# 1. Como instalar Instalar no Linux:
- Executar comando abaixo no shell(prompt de comando) do linux
```
curl -fsSL https://instalador.nirooh.com/install.sh | sh
```

# 2. Como instalar Instalar no Windows:

 ---  em breve  ---
